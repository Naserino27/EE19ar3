const heading = document.querySelector("h1");
heading.innerHTML = "Goodbye World";
console.log(heading.innerHTML);

const listitems = document.querySelectorAll("li");
listitems.array.forEach(element => {
    console.log(element);
});

const userInput = document.querySelector("input[type='text']");
userInput.getAttribute;

let newImage = document.createElement;
newImage.setAttribute("src", "./images/myCoolPicture.jpeg");
newImage.setAttribute("alt", "A picture of something cool");
newImage.setAttribute("coolImageClass");
document.querySelector("body").prepend(newImage);

let myList = document.createElement("ul");
let myListItem = document.createElement("li");

//add event listener

const loginButton = document.querySelector("button.login");
loginButton.addEventListener("click", loginuser);

function (params) {
    
}